apiVersion: v1
kind: Pod
metadata:
  name: simple-webapp-color
spec:
 containers:
 - name: simple-webapp-color
   image: lextsy/color-app
   ports:
   - containerPort: 8080
   env:
   - name: APP_COLOR
     value: pink
